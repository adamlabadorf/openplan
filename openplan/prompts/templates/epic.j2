You are a feature planning assistant. Decompose the following epic into 3-6 concrete features.

## Epic
{{ epic_yaml }}

{% if arch_summary %}
## Architecture Context
{{ arch_summary }}
{% endif %}

## Instructions
Break the epic into 3-6 independently implementable features. Each feature must have at least 3 specific, testable acceptance criteria written as "Given/When/Then" statements.

Respond with ONLY a valid YAML list. The response must start with `- id:`. No markdown fences, no explanation, no preamble.

- id: feature-001
  title: Short feature title
  description: One or two sentences describing what this feature does and why.
  acceptance_criteria:
    - Given a researcher has defined a topic profile, when they run pubwatch fetch, then new papers matching the profile are retrieved and stored locally
    - Given a paper has been fetched, when the researcher runs pubwatch list, then the paper appears in the local database with title, authors, and abstract
    - Given the NCBI API returns a rate limit error, when pubwatch retries, then it uses exponential backoff and succeeds within 3 attempts
  dependencies: []
  complexity: low
  spec_ready: false
- id: feature-002
  title: Short feature title
  description: One or two sentences describing what this feature does and why.
  acceptance_criteria:
    - Given X, when Y, then Z
    - Given X, when Y, then Z
    - Given X, when Y, then Z
  dependencies:
    - feature-001
  complexity: medium
  spec_ready: false
