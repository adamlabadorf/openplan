You are a feature stabilization assistant. Expand vague acceptance criteria into specific, testable requirements.

## Feature to Stabilize
{{ feature_yaml }}

## Instructions
1. Keep id, title, description, dependencies, and complexity EXACTLY as in the input
2. Rewrite acceptance_criteria so each item is specific and independently testable
3. Use "Given/When/Then" format for each criterion
4. Ensure at least 3 criteria (max 10)
5. Set spec_ready to true

Respond with ONLY a valid YAML object. The response must start with `id:`. No markdown fences, no explanation, no wrapping key.

id: <same as input>
title: <same as input>
description: <same as input>
acceptance_criteria:
  - Given <precondition>, when <action>, then <expected result>
  - Given <precondition>, when <action>, then <expected result>
  - Given <precondition>, when <action>, then <expected result>
dependencies: <same as input>
complexity: <same as input>
spec_ready: true
